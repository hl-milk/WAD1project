<!DOCTYPE html>
<html>
<head>
    <title>Account Settings - MovReview</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <nav>
            <div class="nav-logo">MovReview</div>
            <div class="nav-links">
                <a href="/watchlist" class="btn btn-outline">My Watchlist</a>
                <a href="/watched" class="btn btn-outline">Watched</a>
                <a href="/account-settings" class="btn btn-outline">Settings</a>
                <a href="/logout" class="btn btn-danger">Logout</a>
            </div>
        </nav>
    </header>

    <div class="container">
        <div class="flex justify-between items-center mb-20">
            <h1>Account Settings</h1>
            <a href="/home" class="btn btn-outline">Back to Home</a>
        </div>

        <div class="card">
            <% if (typeof(e) === typeof("a")) { %>
                <div class="alert alert-danger"><%=e%></div>
            <% } %>
            <% if (e === 1) { %>
                <div class="alert alert-success">Successfully updated password!</div>
            <% } %>
            <% if (e === 2) { %>
                <div class="alert alert-success">Successfully updated bio!</div>
            <% } %>

            <h3>Edit Bio</h3>
            <form method="post" action="/account-settings">
                <input type="hidden" name="type" value="bio">
                <div class="form-group">
                    <label for="bio">Bio</label>
                    <textarea id="bio" name="bio" rows="4" style="width: 100%; border-radius: 5px; border: 1px solid var(--border-color); padding: 10px; background: var(--card-bg); color: var(--text-color);"><%= user.bio %></textarea>
                </div>
                <div class="flex gap-10">
                    <button type="submit" class="btn btn-primary">Update bio</button>
                </div>
            </form>

            <hr style="margin: 30px 0; border: 0; border-top: 1px solid var(--border-color);">

            <h3>Change Password</h3>
            <form method="post" action="/account-settings">
                <input type="hidden" name="type" value="password">
                <div class="form-group">
                    <label for="password">New Password</label>
                    <input type="password" id="password" name="password">
                </div>
                <div class="form-group">
                    <label for="cpassword">Confirm Password</label>
                    <input type="password" id="cpassword" name="cpassword">
                </div>
                <div class="flex gap-10">
                    <button type="submit" class="btn btn-primary">Change password</button>
                </div>
            </form>
        </div>

        <div class="card" style="border-top: 5px solid var(--danger-color);">
            <h3 style="color: var(--danger-color);">Account Management</h3>
            <p>Once you delete your account, there is no going back. Please be certain.</p>
            <div class="flex gap-10 mt-20">
                <a href="/delete-account" class="btn btn-danger">Delete account</a>
            </div>
        </div>
    </div>
    <script>
        if (urlParams.get('status') === 'databaseerror') {
            alert("Error retrieving user!");
            window.history.replaceState(null, null, window.location.pathname);
        }
    </script>
</body>
</html>